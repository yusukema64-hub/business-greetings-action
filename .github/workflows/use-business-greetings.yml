name: Use Business Greetings

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Business Greetings
        uses: ./
        with:
          name: 'Udacity Learner'